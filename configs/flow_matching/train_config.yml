seed: 42
num_epochs: 40
accum_steps: 8
checkpoint_dir: "checkpoints/flow_matching"
checkpoint_freq: 10
optimizer:
  lr: 5e-4
  betas: [0.9, 0.999]
  weight_decay: 1e-4
scheduler:
  warmup_epochs: 5
early_stopping: null
data:
  batch_size: 32
  data_dir: "data"
